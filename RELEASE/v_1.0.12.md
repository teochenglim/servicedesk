# v1.0.12 — Persona-Driven UX Redesign: AI Ticket Intelligence + Knowledge Base

Theme: the AI/Agent collaboration layer of the redesign in [DESIGN/08_design_ux.md](../DESIGN/08_design_ux.md), built on top of the RBAC/stage-tracking/attachments/rich-text/Customer-view/Manager/ServiceDeskAdmin foundation shipped across [v_1.0.10.md](v_1.0.10.md) and [v_1.0.11.md](v_1.0.11.md). This absorbs [v_3.0.0.md](v_3.0.0.md)'s "AI/Agent collaboration" placeholder — the AI Ticket Intelligence Panel and drafting work below answer the open questions that file raised, so v_3.0.0 is superseded by this file rather than being a separate initiative.

## Already shipped (don't re-do — see v_1.0.10.md / v_1.0.11.md)

RBAC capability model, stage-tracking overlay + Progress Bar, `RoleAgent` + API-token auth, attachments, Toast UI Editor rich text, Customer view rebuild. Manager dashboard/activity list and ServiceDeskAdmin + Sudo-as are tracked in [v_1.0.11.md](v_1.0.11.md) — not yet built as of this file's creation; update this line once they land, since the AI panel's audit attribution and Sudo-as interact (an AI-assisted edit made during a sudo session should attribute correctly).

## Planned / not started

- **AI-assisted drafting + AI Ticket Intelligence Panel** ([DESIGN/08](../DESIGN/08_design_ux.md) §8.8–8.9): needs an LLM/framework decision first (open question carried over from v_3.0.0.md) — recommend a small `internal/llm.Client` interface seam regardless of vendor, mirroring the existing `EventPublisher`/`WebhookDispatcher`/`WorkflowTrigger` seam pattern in `internal/service/interfaces.go`. Versioned snapshots (`TicketAISnapshot`), human-edit-outranks-regeneration semantics, Suggest Split.
- **Knowledge Base feedback loop** ([DESIGN/08](../DESIGN/08_design_ux.md) §8.10): human curation gate before anything publishes to a customer-facing surface — nothing auto-publishes. Suggestion-matching on the structured extraction at submission and triage, not raw text.

## Suggested build order

1. AI draft buttons + AI Ticket Intelligence Panel (needs the LLM/framework decision above before starting).
2. Knowledge Base feedback loop (needs a real backlog of curated snapshots from step 1).

Not in scope: the existing Problems/RCA feature (`/problems`) is untouched.
