# v1.0.9 — README screenshot

Theme: a one-line documentation change — add a UI screenshot to `README.md` so the project reads as a working product on first glance (e.g. on GitHub), not just a wall of text.

## Shipped

- `images/servicedesk.png` (2996×1536 screenshot of the Thoughtworks-themed UI from v1.0.7) referenced via `![ServiceDesk](images/servicedesk.png)` directly under the `# ServiceDesk` title in `README.md`, above the one-line description.

## Explicitly deferred

- No image optimization/resizing pass on the PNG (370KB) — fine for a README, would only matter if this were served at runtime.
- No additional screenshots (e.g. ticket detail, admin pages) — one hero image at the top is enough for this pass; more can be added later if wanted.
