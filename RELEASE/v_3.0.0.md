# v3.0.0 — AI/Agent collaboration (superseded by v1.0.11)

**Superseded**: this file's scope is now answered by [v_1.0.11.md](v_1.0.11.md)'s AI-assisted drafting + AI Ticket Intelligence Panel + Knowledge Base sections, which resolve the open questions below as part of the same persona-driven redesign rather than a separate initiative. Kept here for history; don't plan new work directly against this file.

Theme: let AI agents work alongside human Engineers within the ticketing flow, rather than treating automation as fixed rule/runbook scripts.

## Planned / not started

Nothing has been designed yet — this is a placeholder capturing the stated direction so it isn't lost. Before implementing anything here, this needs a proper brainstorming pass to nail down at minimum:

- What an "agent" actually does in this system: triage suggestions, draft responses, auto-resolve certain ticket classes, something else?
- Whether agents act through the existing Runbook Hook step interpreter (a new step type, e.g. `llm_call`) or need a different integration point.
- How agent actions get attributed in the audit trail (`event_logs.actor_id` currently assumes a human/system `User` row — does an agent get a synthetic user, like the existing `system` bootstrap actor used for automation-authored notes?).
- Guardrails: what an agent is allowed to do autonomously vs. propose-for-human-approval (the existing `approval` workflow step type may be reusable here).
- Which LLM/agent framework, if any, and how credentials/config for it fit into `internal/config`.

Do not start coding against this file — it's intentionally underspecified until someone works through those questions.
